import * as React from "react";
import { FC, useSate } from "react";
import { Spin } from "antd";
import { LoadingOutlined } from "@ant-design/icons";
import { City } from "../../models";
import { getIcon } from "../../api";
import "./Card.scss";

const Card: FC<Props> = ({  city }) => {
    return (
        <div></div>
    )
}
export default Card;